import { PlaceholderPage } from "@/components/layout/PlaceholderPage";
import { pageContent } from "@/data/pages";

export default function ScanPage() {
  return <PlaceholderPage content={pageContent.scan} showSecurityNotice />;
}
